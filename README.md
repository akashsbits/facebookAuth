A simple example of authentication with Facebook and PassportJS

## Quick Start

```bash
$ git clone https://github.com/akashsbits/facebookAuth.git
$ cd facebookAuth
$ npm install
$ npm start
```

Navigate to [`http://localhost:8000`](http://localhost:8000)